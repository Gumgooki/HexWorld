import React from 'react';
import { Link } from 'react-router-dom';

const Customizer = () => {
  return (
    <div className="hex-custom">
      <button>+PLUS+</button>
    </div>
  );
};

export default Customizer;
